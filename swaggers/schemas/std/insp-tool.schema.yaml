# STD_INSP_TOOL_TB

# Columns
insp_tool_id: 
  type: object
  properties: 
    insp_tool_id:
      description: 검사구ID
      type: number
      format: int4

factory_id: 
  type: object
  properties: 
    factory_id:
      description: 공장ID
      type: number
      format: int4
      
insp_tool_cd: 
  type: object
  properties: 
    insp_tool_cd:
      description: 검사구코드
      type: string
      format: varchar(20)

insp_tool_nm: 
  type: object
  properties: 
    insp_tool_nm:
      description: 검사구명
      type: string
      format: varchar(50)

uuid:
  type: object
  properties: 
    uuid:
      description: 검사구UUID
      type: string
      format: uuid

# Uuids
insp_tool_uuid:
  type: object
  properties: 
    insp_tool_uuid:
      description: 검사구UUID
      type: string
      format: uuid

factory_uuid:
  type: object
  properties: 
    factory_uuid:
      description: 공장UUID
      type: string
      format: uuid