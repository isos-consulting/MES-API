# STD_WORKER_GROUP_TB

# Columns
worker_group_id: 
  type: object
  properties: 
    worker_group_id:
      description: 작업조ID
      type: number
      format: int4

factory_id: 
  type: object
  properties: 
    factory_id:
      description: 공장ID
      type: number
      format: int4
      
worker_group_cd: 
  type: object
  properties: 
    worker_group_cd:
      description: 작업조코드
      type: string
      format: varchar(20)

worker_group_nm: 
  type: object
  properties: 
    worker_group_nm:
      description: 작업조명
      type: string
      format: varchar(50)

uuid:
  type: object
  properties: 
    uuid:
      description: 작업조UUID
      type: string
      format: uuid

# Uuids
worker_group_uuid:
  type: object
  properties: 
    worker_group_uuid:
      description: 작업조UUID
      type: string
      format: uuid

factory_uuid:
  type: object
  properties: 
    factory_uuid:
      description: 공장UUID
      type: string
      format: uuid