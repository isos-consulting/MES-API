# STD_FACTORY_TB

# Columns
factory_id: 
  type: object
  properties: 
    factory_id:
      description: 공장ID
      type: number
      format: int4

factory_cd: 
  type: object
  properties: 
    factory_cd:
      description: 공장코드
      type: string
      format: varchar(20)

factory_nm: 
  type: object
  properties: 
    factory_nm:
      description: 공장명
      type: string
      format: varchar(50)

uuid:
  type: object
  properties: 
    uuid:
      description: 공장UUID
      type: string
      format: uuid

# Uuids
factory_uuid:
  type: object
  properties: 
    factory_uuid:
      description: 공장UUID
      type: string
      format: uuid