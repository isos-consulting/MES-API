# STD_EQUIP_TB

# Columns
equip_id:
  type: object
  properties:
    equip_id:
      description: 설비ID
      type: number
      format: int4

factory_id:
  type: object
  properties:
    factory_id:
      description: 공장ID
      type: number
      format: int4

equip_type_id:
  type: object
  properties:
    equip_type_id:
      description: 설비유형ID
      type: number
      format: int4

equip_cd:
  type: object
  properties:
    equip_cd:
      description: 설비코드
      type: string
      format: varchar(20)

equip_nm:
  type: object
  properties:
    equip_nm:
      description: 설비명
      type: string
      format: varchar(50)

workings_id:
  type: object
  properties:
    workings_id:
      description: 작업장ID
      type: number
      format: int4

manager_emp_id:
  type: object
  properties:
    manager_emp_id:
      description: 관리자(정)ID
      type: number
      format: int4

sub_manager_emp_id:
  type: object
  properties:
    sub_manager_emp_id:
      description: 관리자(부)ID
      type: number
      format: int4

equip_no:
  type: object
  properties:
    equip_no:
      description: 설비관리번호
      type: string
      format: varchar(25)

equip_grade:
  type: object
  properties:
    equip_grade:
      description: 설비등급
      type: string
      format: varchar(10)

equip_model:
  type: object
  properties:
    equip_model:
      description: 설비모델명
      type: string
      format: varchar(50)

equip_std:
  type: object
  properties:
    equip_std:
      description: 설비규격
      type: string
      format: varchar(50)

equip_spec:
  type: object
  properties:
    equip_spec:
      description: 설비제원
      type: string
      format: varchar(50)

voltage:
  type: object
  properties:
    voltage:
      description: 전압
      type: string
      format: varchar(10)

manufacturer:
  type: object
  properties:
    manufacturer:
      description: 제조사
      type: string
      format: varchar(50)

purchase_partner:
  type: object
  properties:
    purchase_partner:
      description: 구매업체
      type: string
      format: varchar(50)

purchase_date:
  type: object
  properties:
    purchase_date:
      description: 구매일자
      type: string
      format: date

purchase_tel:
  type: object
  properties:
    purchase_tel:
      description: 구매업체연락처
      type: string
      format: varchar(50)

purchase_price:
  type: object
  properties:
    purchase_price:
      description: 구매금액
      type: number
      format: numeric(19, 6)

use_fg:
  type: object
  properties:
    use_fg:
      description: 사용여부
      type: boolean
      format: boolean

prd_fg:
  type: object
  properties:
    prd_fg:
      description: 생산설비여부
      type: boolean
      format: boolean

remark:
  type: object
  properties:
    remark:
      description: 비고
      type: string
      format: varchar(250)

uuid:
  type: object
  properties:
    uuid:
      description: 설비UUID
      type: string
      format: uuid

# Uuids
equip_uuid:
  type: object
  properties:
    equip_uuid:
      description: 설비UUID
      type: string
      format: uuid

factory_uuid:
  type: object
  properties:
    factory_uuid:
      description: 공장UUID
      type: string
      format: uuid

equip_type_uuid:
  type: object
  properties:
    equip_type_uuid:
      description: 설비유형UUID
      type: string
      format: uuid

workings_uuid:
  type: object
  properties:
    workings_uuid:
      description: 작업장UUID
      type: string
      format: uuid

manager_emp_uuid:
  type: object
  properties:
    manager_emp_uuid:
      description: 관리자(정)UUID
      type: string
      format: uuid

sub_manager_emp_uuid:
  type: object
  properties:
    sub_manager_emp_uuid:
      description: 관리자(부)UUID
      type: string
      format: uuid

# Others
manager_emp_cd: 
  type: object
  properties: 
    manager_emp_cd:
      description: 관리자(정) 사원코드
      type: string
      format: varchar(20)

manager_emp_nm: 
  type: object
  properties: 
    manager_emp_nm:
      description: 관리자(정) 사원명
      type: string
      format: varchar(50)

sub_manager_emp_cd: 
  type: object
  properties: 
    sub_manager_emp_cd:
      description: 관리자(부) 사원코드
      type: string
      format: varchar(20)

sub_manager_emp_nm: 
  type: object
  properties: 
    sub_manager_emp_nm:
      description: 관리자(부) 사원명
      type: string
      format: varchar(50)
