# ADM_TRAN_TYPE_TB

# Columns
tran_type_id:
  type: object
  properties:
    tran_type_id:
      description: 수불유형ID
      type: number
      format: int4

tran_type_cd:
  type: object
  properties:
    tran_type_cd:
      description: 수불유형코드
      type: string
      format: varchar(50)

tran_type_nm:
  type: object
  properties:
    tran_type_nm:
      description: 수불유형명
      type: string1
      format: varchar(50)

sortby:
  type: object
  properties:
    sortby:
      description: 정렬
      type: number
      format: int4

remark:
  type: object
  properties:
    remark:
      description: 비고
      type: string
      format: varchar(250)

uuid:
  type: object
  properties:
    uuid:
      description: 수불유형UUID
      type: string
      format: uuid

# Uuids
tran_type_uuid:
  type: object
  properties:
    tran_type_uuid:
      description: 수불유형UUID
      type: string
      format: uuid
