# ADM_DEMAND_TYPE_TB

# Columns
demand_type_id:
  type: object
  properties:
    demand_type_id:
      description: 자재출고요청유형ID
      type: number
      format: int4

demand_type_cd:
  type: object
  properties:
    demand_type_cd:
      description: 자재출고요청유형코드
      type: string
      format: varchar(50)

demand_type_nm:
  type: object
  properties:
    demand_type_nm:
      description: 자재출고요청유형명
      type: string1
      format: varchar(50)

sortby:
  type: object
  properties:
    sortby:
      description: 정렬
      type: number
      format: int4

uuid:
  type: object
  properties:
    uuid:
      description: 자재출고요청유형UUID
      type: string
      format: uuid

# Uuids
demand_type_uuid:
  type: object
  properties:
    demand_type_uuid:
      description: 자재출고요청유형UUID
      type: string
      format: uuid
