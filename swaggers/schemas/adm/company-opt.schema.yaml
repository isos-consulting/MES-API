# ADM_COMPANY_OPT_VW

# Columns
company_opt_id:
  type: object
  properties:
    company_opt_id:
      description: 회사옵션ID
      type: number
      format: int4

company_opt_cd:
  type: object
  properties:
    company_opt_cd:
      description: 회사옵션코드
      type: string
      format: varchar(20)

company_opt_nm:
  type: object
  properties:
    company_opt_nm:
      description: 회사옵션명
      type: string
      format: varchar(50)

remark:
  type: object
  properties:
    remark:
      description: 비고
      type: string
      format: varchar(250)

val:
  type: object
  properties:
    val:
      description: 값
      type: string
      format: varchar(50)

val_opt:
  type: object
  properties:
    val_opt:
      description: 상세옵션
      type: string
      format: varchar(50)

sortby:
  type: object
  properties:
    sortby:
      description: 정렬
      type: number
      format: int4

# Uuids
company_opt_uuid:
  type: object
  properties:
    company_opt_uuid:
      description: 회사옵션UUID
      type: string
      format: uuid
