# ADM_INSP_DETAIL_TYPE_TB

# Columns
insp_detail_type_id:
  type: object
  properties:
    insp_detail_type_id:
      description: 세부검사유형ID
      type: number
      format: int4

insp_detail_type_cd:
  type: object
  properties:
    insp_detail_type_cd:
      description: 세부검사유형코드
      type: string
      format: varchar(20)

insp_detail_type_nm:
  type: object
  properties:
    insp_detail_type_nm:
      description: 세부검사유형명
      type: string
      format: varchar(50)

insp_type_id:
  type: object
  properties:
    insp_type_id:
      description: 검사유형ID
      type: number
      format: int4

worker_fg:
  type: object
  properties:
    worker_fg:
      description: 작업자 검사 여부
      type: boolean
      format: boolean

inspector_fg:
  type: object
  properties:
    inspector_fg:
      description: 검사자 검사 여부
      type: boolean
      format: boolean

sortby:
  type: object
  properties:
    sortby:
      description: 정렬
      type: number
      format: int4

uuid:
  type: object
  properties:
    uuid:
      description: 세부검사유형UUID
      type: string
      format: uuid
# Uuids
insp_type_uuid:
  type: object
  properties:
    insp_type_uuid:
      description: 검사유형UUID
      type: string
      format: uuid
